<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Users</title>
</head>
<body>
    <ul>
        <% rows.forEach(function(row) { %>
            <li>ID: <%= row.id %>, Name: <%= row.name %> <%= row.language %></li>
        <% }); %>
    </ul>
</body>
</html>
